# button for deleting a collections
# collection -- collection to be deleted
<%= button_to label, collections.collection_path(collection.id), :method=>:delete, :class=>"btn btn-primary collection-delete", 'data-behavior'=>'hydra-collections', :id=>'hydra-collection-add' %>

